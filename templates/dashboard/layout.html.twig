{% extends 'dashboard/base_dashboard.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/layout.css') }}">
{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-2 sidebar">
            {% block sidebar %}
                <nav class="nav flex-column">
                    <a class="nav-link" href="{{ path('app_dashboard') }}">
                        <i class="fas fa-home me-2"></i> Accueil
                    </a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <div class="section-title">Administration</div>

                        <a class="nav-link" href="{{ path('app_users') }}">
                            <i class="fas fa-users-cog me-2"></i> Gestion des utilisateurs
                        </a>
                        <a class="nav-link" href="{{ path('app_admin_promotions') }}">
                            <i class="fas fa-graduation-cap me-2"></i> Gestion des promotions
                        </a>
                        <a class="nav-link" href="{{ path('app_entreprises') }}">
                            <i class="fas fa-building me-2"></i> Gestion des entreprises
                        </a>
                        <a class="nav-link" href="{{ path('app_offres') }}">
                            <i class="fas fa-briefcase me-2"></i> Gestion des offres
                        </a>
                    {% endif %}

                    {% if is_granted('ROLE_PILOTE') %}
                        <div class="section-title">Gestion</div>
                        <a class="nav-link" href="{{ path('app_dashboard') }}">
                            <i class="fas fa-home me-2"></i> Accueil
                        </a>
                        <a class="nav-link" href="{{ path('app_promotions_stage') }}">
                            <i class="fas fa-graduation-cap me-2"></i> Gestion des Promotions
                        </a>
                        <a class="nav-link" href="{{ path('app_etudiants_stage') }}">
                            <i class="fas fa-user-graduate me-2"></i> Gestion des Étudiants
                        </a>
                        <a class="nav-link" href="{{ path('app_entreprises_stage') }}">
                            <i class="fas fa-building me-2"></i> Gestion des Entreprises
                        </a>
                        <a class="nav-link" href="{{ path('app_offres_stage') }}">
                            <i class="fas fa-briefcase me-2"></i> Gestion des Offres
                        </a>
                    {% endif %}
                </nav>
            {% endblock %}
        </div>

        <!-- Main Content -->
        <div class="col-md-10 main-content">
            {% block dashboard_content %}{% endblock %}
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap5.min.js"></script>
{% endblock %} 